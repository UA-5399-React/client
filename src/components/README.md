# Components

Reusable UI components that can be used throughout the application.

## Structure

- Each component should have its own folder
- Include component file, styles, tests, and types in the same folder
- Example: `Button/Button.tsx`, `Button/Button.module.css`, `Button/Button.test.tsx`
