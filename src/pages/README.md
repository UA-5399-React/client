# Pages

Page-level components that represent different routes/views in the application.

Each page component typically:

- Composes multiple smaller components
- Handles page-specific logic
- Manages page-level state
